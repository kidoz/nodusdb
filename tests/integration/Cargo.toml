[package]
name = "nodus_integration_tests"
version.workspace = true
edition.workspace = true
authors.workspace = true
license.workspace = true
repository.workspace = true
publish = false

[lints]
workspace = true

[dependencies]
nodus_config = { path = "../../crates/nodus_config" }
nodus_testkit = { path = "../../crates/nodus_testkit" }
reqwest.workspace = true
serde_json.workspace = true
# Serializes the heavy in-process multi-node cluster tests so they don't all run
# at once (dozens of servers) and starve each other's election/replication timing.
serial_test = "3"
tokio.workspace = true
tokio-postgres.workspace = true
