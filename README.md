# NodusDB
PostgreSQL-wire-compatible distributed SQL database in Rust.

## Getting Started
To run the server locally:
```bash
cargo run --bin nodus_server
```

To run the CLI:
```bash
cargo run --bin nodus_cli -- help
```